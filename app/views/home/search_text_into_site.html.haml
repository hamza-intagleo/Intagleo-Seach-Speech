%h5
  %b Google API Search Results will appear below:

  %table.table#search_data_table
    %thead
      %tr
        %th Title
        %th Link
      - @response.items.each do |item|
        %tr
          %td
            = item.title
          %td
            = link_to item.link, item.link, target: '_blank'

